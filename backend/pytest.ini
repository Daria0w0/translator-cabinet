[pytest]
testpaths = test
markers =
    unit: модульные тесты без БД и внешних зависимостей (только моки)
    integration: интеграционные тесты с SQLite и TestClient
    slow: тесты которые работают долго
addopts = --tb=short -q